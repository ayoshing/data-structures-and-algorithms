// Linked List is a linear collection of data elements, order is not given by their physical placement in memory.
// every Linked List Node has a null
// null by itself is a valid Linked List
// the first node is called the HEAD
// Linked List basic operations: Insert, Search, Delete

class LinkedListNode {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

class LinkedList {
  constructor() {
    this.head = head;
    this.tail = tail;
    this.length = length;
  }
}
